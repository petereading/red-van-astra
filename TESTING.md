# 紅Van 驗證記錄

驗證日期：2026-09-15。此記錄對應擴充街區、日夜街景、街道物件、小巴及駕駛特效更新。

## 自動測試

`npm test`：**30 項全部通過**。

- 300 個隨機種子的乘客、下游落客、轉彎避讓及重現性。
- 停車採用真實左前車門位置；允許車身部分超出方塊及不同角度，車門離開方塊或仍在行車則不會上落客。
- 80 km/h 煞停不超過 0.7 秒、滑行少於 8 米；按住剎車不會自行轉為倒車。
- 實時評級隨路線及接送進度變化；未完成路線或空車衝線不會取得 S 級。
- 加速、倒車速度限制、超速時間累積及 Rapier 高速撞牆。
- 真正遊戲狀態中的上落客、車門單次違規、暫停、逾時、復位，以及尾站必須完成路線和開關門流程。
- 行人跳開後站穩、車況不受行人避讓影響；候車乘客仍在等車，重複接觸不會刷扣分。
- 有乘客時高速急煞單次扣分；暢玩模式的碰撞、急煞及復位不會扣分、扣秒或造成車損失敗。
- 紅燈持續五秒、車輛停線及完整車身間距；闖紅燈只計一次違規。
- 400 秒車流模擬，包括不同車長及超過 20 次車流循環；沒有路線端點重疊。
- 正式自動示範入口，包括倒數、正常遊戲時鐘、完整路線及結算。
- 錄影主畫面五秒、遊戲聲音、結算八秒後停止，以及預覽／下載流程。
- 取消分享視窗、拒絕授權、選錯來源、不支援錄影、格式選擇及保存部分片段。
- 示範及暢玩模式不改寫最佳紀錄；暢玩模式成功／漏掉指定服務會顯示不同任務結果。

- 每條支路均可循道路圖返回下一個未完成路段；導航路徑沿街道、不穿越街區，走錯路或抄近路會顯示紅箭嘴。
- 超過一千個道路中心取樣點沒有被建築／街道物件堵住；支路駕駛不會刷主路線進度。
- 物件碰撞彈開、短暫離地、車損及新一局還原；移除過近彎位的兩件物件。
- 日夜模式切換窗戶／招牌發光與街燈；局部街燈限制六盞，火花池固定 96 顆。
- 車身傾側少於約六度、小幅離地少於 16 cm；停車時不抖動，效果不改變物理及車門位置。
- 八個男女短音檔均為有效非空 PCM WAV，長度約 0.2–0.5 秒。

## 完整路線

執行 `npm run test:drive -- 8521986 42 12345`。使用真正的 `Game.step`、Rapier 碰撞、交通燈、車流及乘客流程。車手只輸入油門、轉向、剎車及車門操作，沒有瞬移、修改成績或延長 180 秒時限。

| 路況種子 | 上車／送達 | 剩餘時間 | 超速 | 車門／紅燈／急煞違規 | 人／車／物件碰撞 | 得分／評級 |
| --- | --- | --- | --- | --- | --- | --- |
| 8521986 | 10 / 10 | 25.50 秒 | 0 秒 | 0 / 0 / 0 | 0 / 0 / 0 | 951 / S |
| 42 | 10 / 10 | 25.10 秒 | 0 秒 | 0 / 0 / 0 | 0 / 0 / 0 | 950 / S |
| 12345 | 10 / 10 | 29.28 秒 | 0 秒 | 0 / 0 / 0 | 0 / 0 / 0 | 959 / S |

三局均通過全部 36 個路線檢查點、沒有錯過服務或使用復位，車況為 100%。正式示範入口另以種子 8521986 通過完整倒數及全程，同樣送達十人、剩餘 25.50 秒。

這些結果驗證限時可達成及相關規則運作，不代表已逐一測試所有隨機局或人工操作手感。

## 畫面及介面檢查

工作區恢復後，雲端 Chrome 仍回報 `GL_VENDOR = Disabled, GL_RENDERER = Disabled`，無法建立 WebGL context。因此沒有聲稱完成本輪 3D 場景、實際幀率、人工鍵盤／觸控駕駛、音效聆聽或真實影片編碼驗證。

使用同一瀏覽器，以不啟動 3D 的臨時測試頁載入真正的 `UI` 及樣式，檢查 1280×800、390×844 及 844×390 的主畫面、模式選擇、HUD 與結算。檢查新增日夜按鈕、選中狀態及手機直／橫向排版；保留上輪導航與交通燈提示的間距修正。此測試使用介面用的示例數值，並非實際試玩。

暢玩 HUD 隱藏分數、評級與車況；結算只顯示接送任務、剩餘時間及成功與否。模式按鈕及選單／遊戲／結算切換已透過實際 DOM 互動檢查。

小巴招牌改在字體載入後才建立，避免初次載入使用缺字的字體。新增幾何及材質已通過程式建構、道路淨空取樣和 TypeScript 檢查，但其最終 3D 光照效果及 M1／手機效能仍需實機確認。

## 建置及測試入口

TypeScript 檢查及 Vite 正式建置通過。暫存介面檢查頁放在忽略的 `.qa/`，不包含在正式產物或來源提交。

```sh
npm test
npm run test:drive -- 8521986 42 12345
npm run build
```

開發伺服器的 `?qa=1` 提供完整路線自動駕駛、停站、超速、逾時及撞車入口；正式建置不啟用 QA 面板。

錄影測試的 MediaRecorder／MediaStream 使用模擬物件，不涉及咪高峰或系統聲音。真實 Chrome／Edge 分享視窗、WebM／MP4 播放及 macOS 螢幕錄影權限未在本環境完成實測。
