# 第一版驗證記錄

驗證日期：2026-09-14。

## 已通過

`npm test`：10 項測試全部通過。

- 300 個隨機種子的乘客數量、下游落客安排、轉彎避讓及種子重現。
- 小巴整車、方向與速度的停車方塊判定。
- 全路線投影及轉彎位置。
- S–D 分級、未完成路線及空車衝線的評分限制。
- 加速、剎車、倒車及 80 km/h 以上速度。
- Rapier 高速撞牆及碰撞事件，沒有穿牆。
- 真正遊戲流程中的開門上客、關門行車、危險車門操作單次扣分、暫停及逾時。
- 正確位置落客，以及尾站必須先完成路線和開關門流程。
- 移動中中斷上客、超速時間累積及復位扣秒扣分。
- 候車乘客碰撞、重複碰撞不刷扣分、重玩重設乘客及車況。

## 完整路線測試

使用 `Game.step`、實際 Rapier 世界及遊戲的乘客／交通流程。測試車手只輸入油門、剎車、轉向及車門操作；完整路線中沒有使用瞬移，也沒有延長 180 秒時限。

| 路況種子 | 上車／送達 | 剩餘時間 | 超速 | 車門違規 | 碰撞：人／車／物 | 得分／評級 |
| --- | --- | --- | --- | --- | --- | --- |
| 8521986 | 10 / 10 | 33.63 秒 | 0 秒 | 0 | 0 / 0 / 0 | 967 / S |
| 42 | 10 / 10 | 38.17 秒 | 0 秒 | 0 | 1 / 0 / 0 | 826 / A |
| 12345 | 10 / 10 | 38.07 秒 | 0 秒 | 0 | 0 / 0 / 0 | 976 / S |

種子 42 的測試車手沒有主動避讓橫過馬路的行人；碰撞正確造成車損及扣分。這些結果證明正常速度可以在時限內完成路線，並驗證碰撞會影響評級；不代表所有隨機局都已全面測試。

## 尚未完成的實機驗證

本次工作環境的 Cloud Browser 回報 `ERR_BLOCKED_BY_CLIENT`，無法開啟已啟動的遊戲預覽。因此沒有聲稱完成瀏覽器畫面檢查、人工鍵盤／觸控試玩、音效聆聽、Safari 或手機效能驗證。自動化測試在無瀏覽器繪圖的環境執行，不代替這些檢查。

正式建置通過 TypeScript 檢查及 Vite 打包。發布狀態另由部署服務確認。

## 開發測試入口

`npm run dev` 後在網址加上 `?qa=1` 可使用可見的開發測試面板：完整路線自動駕駛、停站、超速／車門、逾時及撞車。面板只在開發模式啟用。

```sh
npm test
npm run test:drive -- 8521986 42 12345
```


## 自動示範與錄影更新（2026-09-14）

`npm test`：17 項全部通過，包含原有 10 項及新增 7 項。

- 錄影主畫面 5 秒、啟動自動車手、計分後 8 秒停止及最終資料匯出。
- 混入遊戲聲音，不要求咪高峰或系統聲音。
- 分享視窗仍開啟時取消，延遲返回的串流會釋放。
- 拒絕分享、選到整個螢幕、瀏覽器不支援及格式選擇。
- 瀏覽器停止分享時保存部分影片，取消延遲啟動。
- 示範成績不寫入個人最佳；普通遊戲仍可更新紀錄。
- 正式示範入口（含開車倒數、非 QA 模式）以正常物理步進完成整條路線：10 名送達，剩餘 33.63 秒，無超速、車門違規或復位；碰撞 1 名行人，正常扣分。

前六項錄影相關測試使用 MediaRecorder／MediaStream 模擬物件驗證流程，未驗證真實影片編碼、畫面或音軌。完整示範路線使用真正 Rapier 世界及 Game.start／Game.step，但不包含瀏覽器繪圖。

本輪已能開啟遊戲預覽，但雲端 Chrome 回報 `GL_VENDOR = Disabled, GL_RENDERER = Disabled`，無法建立 WebGL context。故仍未完成本機 Chrome／Edge 分享視窗、實際 WebM／MP4 播放、音效聆聽及 macOS 權限流程的人工驗證。
